/**
 * add unit tests for UserPolicy:
 * - view allows self, denies others
 * - delete allows self, denies others
 * - before hook grants admin bypass (once roles are introduced)
 *
 * See tests/bootstrap.ts for the Japa + AdonisJS setup.
 */
export {}
