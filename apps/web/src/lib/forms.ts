// TODO(forms): add form management library when complex forms arrive
// - Choose: React Hook Form or TanStack Form + Zod
// - Add Field/Form wrapper components using shadcn Field primitives
// - Define validation patterns (client + server sync with VineJS)
// - Update AGENTS.md to remove "No form library yet" note

export function useForm() {
  return { handleSubmit: () => {} }
}